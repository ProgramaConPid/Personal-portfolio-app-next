// Import the Hero component for the main landing section
import Hero from "@/components/layout/Hero/Hero";

/**
 * Home page component for the portfolio app.
 * Renders the Hero section as the main content.
 */
export default function Home() {
  return <Hero />;
}
